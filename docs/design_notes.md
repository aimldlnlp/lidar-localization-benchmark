# Design Notes

- Keep the CPU reference path correct and deterministic before enabling any CUDA batching.
- Add GPU acceleration only for batched scan simulation and particle likelihood evaluation.
- Keep figures white-background and DejaVu Serif from the start so the visual language remains consistent.

