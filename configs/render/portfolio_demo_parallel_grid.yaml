inherit_from: ../base.yaml

render:
  target_video_name: demo_parallel_grid
  target_run_dir: outputs/runs/noise_sweep_medium
  strict_run_root: true
  allow_fallback: false
  source_experiment: noise_sweep_medium
  fps: 20
  frame_size: [1600, 900]

experiment:
  name: portfolio_demo_parallel_grid
  notes: render the medium-suite parallel grid demo
