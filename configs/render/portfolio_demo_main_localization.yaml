inherit_from: ../base.yaml

render:
  target_video_name: demo_main_localization
  target_run_dir: outputs/runs/noise_sweep_medium
  strict_run_root: true
  allow_fallback: false
  source_experiment: noise_sweep_medium
  fps: 20
  frame_size: [1280, 720]

experiment:
  name: portfolio_demo_main_localization
  notes: render the representative localization demo
