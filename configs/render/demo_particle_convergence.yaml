inherit_from: ../base.yaml

render:
  target_video_name: demo_particle_convergence
  target_run_dir: outputs/runs/particle_count_sweep

experiment:
  name: demo_particle_convergence
  notes: render particle convergence demo video

